source 'http://rubygems.org'
source 'https://3RFCCaafQBDr4qAA2qzQ@gem.fury.io/spritecloud/'


# Add following two lines to your ~/.gemrc or /etc/gemrc:
# install: --no-rdoc --no-ri
# update:  --no-rdoc --no-ri

# Install helper libraries
gem 'rspec'
gem 'gherkin'
gem 'xml-simple'
gem 'mechanize'

# Ruby version specific gems
platforms :ruby_19, :ruby_20 do
  gem 'debugger'
end

# Windows specific
platforms :mswin, :mingw do
  gem 'win32console'
  gem 'term-ansicolor'
end


# Install all the webdriver gems and cucumber
gem 'watir-webdriver'
gem 'watir-webdriver-performance'
gem 'watir-scroll'
gem 'cucumber'

# spriteCloud specific
gem 'regressinator'
gem 'lapis_lazuli', <%= config[:lapis_lazuli][:dependency] %>
