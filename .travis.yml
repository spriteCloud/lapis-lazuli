install:
  - "echo ${PWD} && ls -l && cd ${TEST_DIR} && bundle install"
script:
  - "cd ${TEST_DIR} && COVERAGE=1 bundle exec cucumber"
os:
  - linux
  - osx
language: ruby
rvm:
  - 1.9.3  
  - 2.0.0
notifications:
  email:
    recipients:
      - foss@spritecloud.com
env:
  - TEST_DIR=test
