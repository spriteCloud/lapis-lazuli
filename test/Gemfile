source 'http://rubygems.org'

# Windows specific
platforms :mswin, :mingw do
  gem 'win32console'
  gem 'term-ansicolor'
end

# Locking Watir on 6.1.0 until this fix is deployed:
# https://github.com/watir/watir/commit/472732a3b7899fd6595e966997b3a42922462fbb
gem 'watir', :git => 'https://github.com/watir/watir.git', :ref => '472732a'

# LapisLazul itself; test code should point to the current repo and branch
# repo = `git config --get remote.origin.url`
# branch = `git rev-parse --abbrev-ref HEAD`
# Local gem
# gem "lapis_lazuli", path: './../'

# Development gem
gem 'lapis_lazuli', :git => 'https://github.com/spriteCloud/lapis-lazuli.git', :branch => 'development'

# Production gem
# gem "lapis_lazuli"

# Project specific gems
gem 'watir-scroll'
gem 'xml-simple'
gem 'mechanize'
gem 'simplecov'
gem 'test-unit'